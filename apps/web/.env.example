VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Vercel serverless env vars (used by apps/web/api/create-login.ts). Set these in the
# Vercel project, never in a VITE_* var that gets bundled to the browser:
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key